kind: "BuildConfig"
apiVersion: "v1"
metadata:
  name: "test build"
spec:
  source: 
    type: "Git"
    git:
      uri: "https://github.com/avenging/openshiftbuild"
      ref: "master"
  strategy: 
    type: "Docker"
  output: 
    to:
      kind: "ImageStreamTag"
      name: "hybris-base:latest"
